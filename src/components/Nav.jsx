import React from "react";

// a simple navigation bar with a logo or app name
// a component that returns the nav
const Nav = () => {
  return (
    <nav>
      <h1 className="logo">Todolist App</h1>
    </nav>
  );
};

export default Nav;
